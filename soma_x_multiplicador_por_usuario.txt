programa
{
	
	funcao inicio()
	{
		
		inteiro a, b, c, soma
		escreva("digite o primeiro de número a ser somado: ")
		leia(a)

		escreva("digite o primeiro de número a ser somado: \n")
		leia(b)

		escreva("digite a quantidades de vezes que os valores serão somados\n")
		leia(c)

		soma = a + b * c 

		escreva("A soma total é: ", soma)
		
	}
}